# PM Signal Detection Reference
# Domain knowledge extracted to references/ per DDR-018

feature_requests:
  patterns:
    - "need feature"
    - "requires capability"
    - "looking for"
    - "can {vendor} support"
    - "roadmap item"
    - "when will X be available"

roadmap_dependencies:
  keywords:
    - "blocked by"
    - "waiting for"
    - "depends on"
    - "requires future release"

feasibility_constraints:
  indicators:
    - "not currently supported"
    - "custom development needed"
    - "platform limitation"
    - "architectural constraint"

classification:
  feasible: "Feature exists in current or upcoming release"
  limited: "Partial capability available, requires workaround"
  not_planned: "Feature not on roadmap, requires escalation"
